package com.example.web.config;

// Removed SiteMesh because it depends on javax.servlet, which conflicts with Jakarta stack.
// Keeping an empty placeholder to avoid broken imports if referenced.
public class SiteMeshFilterConfig { }

